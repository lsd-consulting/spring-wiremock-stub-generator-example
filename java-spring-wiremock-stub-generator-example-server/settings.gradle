pluginManagement {
    repositories {
        mavenLocal()
        gradlePluginPortal()
    }
}

rootProject.name = 'java-spring-wiremock-stub-generator-example-server'

include 'api'
include 'application'
