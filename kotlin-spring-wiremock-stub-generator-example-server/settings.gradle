pluginManagement {
    repositories {
        mavenLocal()
        gradlePluginPortal()
    }
}

rootProject.name = 'kotlin-spring-wiremock-stub-generator-example-server'

include 'api'
include 'application'
