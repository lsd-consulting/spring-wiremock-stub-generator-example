pluginManagement {
    repositories {
        mavenLocal()
        gradlePluginPortal()
    }
}

rootProject.name = 'kotlin-spring-wiremock-stub-generator-example-client'

include 'api'
include 'application'
