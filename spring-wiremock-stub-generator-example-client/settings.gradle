pluginManagement {
    repositories {
        mavenLocal()
        gradlePluginPortal()
    }
}

rootProject.name = 'spring-wiremock-stub-generator-example-client'

include 'api'
include 'application'
